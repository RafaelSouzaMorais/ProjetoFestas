# ========================================
# VARIÁVEIS DE AMBIENTE - COOLIFY
# ========================================
# Copie este arquivo para .env e preencha com seus valores

# ----------------------------------------
# FRONTEND
# ----------------------------------------
# Porta pública do frontend (padrão: 80)
FRONTEND_PORT=80

# URL da API para build do frontend (padrão: /api - usa proxy relativo)
# Se usar domínio separado: https://api.seudominio.com/api
VITE_API_URL=/api

# ----------------------------------------
# BACKEND
# ----------------------------------------
# Porta pública do backend (padrão: 3100)
BACKEND_PORT=3100

# Ambiente Node.js (production/development)
NODE_ENV=production

# Porta interna do servidor Express
PORT=3100

# Chave secreta JWT (OBRIGATÓRIO - gere uma chave forte)
# Exemplo geração: openssl rand -base64 32
JWT_SECRET=sua-chave-secreta-aqui-minimo-32-caracteres

# ----------------------------------------
# BANCO DE DADOS POSTGRESQL
# ----------------------------------------
# Host do banco (use 'postgres' se for o container, ou IP/domínio externo)
DB_HOST=postgres

# Porta do PostgreSQL
DB_PORT=5432

# Nome do banco de dados
DB_NAME=projeto_festas

# Usuário do banco (OBRIGATÓRIO)
DB_USER=postgres

# Senha do banco (OBRIGATÓRIO - use senha forte)
DB_PASSWORD=sua-senha-forte-aqui

# Porta pública do Postgres (opcional, para acesso externo)
POSTGRES_PORT=5432

# ========================================
# NOTAS PARA DEPLOY NO COOLIFY
# ========================================
# 1. JWT_SECRET: Gere uma chave forte e segura
# 2. DB_PASSWORD: Use senha forte (letras, números, símbolos)
# 3. DB_USER: Pode manter 'postgres' ou criar usuário específico
# 4. VITE_API_URL: Use /api se frontend e backend estiverem no mesmo domínio
#    Use URL completa (https://api.seudominio.com/api) se em domínios diferentes
# 5. No Coolify, adicione estas variáveis na seção "Environment Variables"
# 6. Exponha a porta do frontend (80) como porta pública principal
# 7. Backend e Postgres podem ficar em rede privada

